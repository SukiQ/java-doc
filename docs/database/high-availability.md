---
title: 高可用
---

# 高可用


## 主从复制的原理？

### 复制流程（三线程）

```
主库：事务提交 → 写 binlog → dump 线程推送
                                  │
从库：IO 线程接收 ──→ 写入 relay log（中继日志）
                                  │
      SQL 线程读 relay log ──→ 重放执行 ──→ 数据一致
```

| 线程 | 所在 | 职责 |
|------|------|------|
| dump 线程 | 主库 | 从 binlog 读取并推送给从库 |
| IO 线程 | 从库 | 接收 binlog，写入 relay log |
| SQL 线程 | 从库 | 重放 relay log 中的事件 |

搭建：主库建复制账号，从库 `change master to ... ; start slave;`

### 三种复制模式

| 模式 | 流程 | 一致性 | 性能 |
|------|------|--------|------|
| 异步（默认） | 主库提交即返回，不等从库 | 崩溃可能丢数据 | 高 |
| 半同步 | ≥1 个从库**收到 binlog**（不是执行完）才返回 | 降级为异步可配 | 中 |
| 全同步 | 所有从库执行完才返回 | 最强 | 差，少用 |

### 主从延迟

**原因**：SQL 线程单线程重放（早期）赶不上主库并发写入；大事务、从库机器差、从库承担大量读。

**危害**：写后立即读从库读不到（"刚下单查不到订单"）。

**缓解**：

```
1. 半同步 + 合理降级策略
2. 5.7+ 并行复制（MTS，按组/WRITESET 并行重放）
3. 大事务拆小，避免单条 binlog 过大
4. 关键读强制走主库（写后一段时间内、或关键业务）
5. 中间件路由：ShardingSphere / ProxySQL 的强制主读
```

### 延迟排查

```sql
show slave status\\G
-- Seconds_Behind_Master：延迟秒数
-- Slave_IO_Running / Slave_SQL_Running：两个线程是否正常
```

### 总结

```
复制：binlog → dump 推送 → IO 线程写 relay log → SQL 线程重放
模式：异步（默认）/半同步/全同步
延迟根因：SQL 线程追不上写 → 半同步 + 并行复制 + 关键读走主
```

面试要点：三线程流程、三种模式的一致性取舍、主从延迟原因与五条缓解手段、写后读的处理。



## 分库分表怎么做？

### 什么时候拆

```
单表：MySQL 建议 2000 万行 / 20GB 内（B+ 树 3 层的量级）
单库：连接数 / CPU / IO 到瓶颈
拆之前先确认：索引优化、SQL 优化、读写分离、归档冷数据都做了吗？
```

### 拆分方式

| 方式 | 说明 | 示例 |
|------|------|------|
| **垂直分库** | 按业务域拆库 | 订单库、用户库、商品库 |
| **垂直分表** | 按列拆：冷热/大小分离 | 主表（常用列）+ 详情表（大字段） |
| **水平分库** | 同构库，行数据分散到多库 | user_db_0 / user_db_1 |
| **水平分表** | 同库内按行拆多表 | order_0 ~ order_7 |

### 分片策略

| 策略 | 做法 | 优缺点 |
|------|------|--------|
| **hash 取模** | `分片 = shard_key % N` | 均匀；**扩容要迁移数据** |
| **range 范围** | 按时间/ID 段分（1-100万→表1） | 扩容容易；**热点集中**（新表压力最大） |
| **基因法** | 把分片基因嵌入 ID（如订单号尾号带用户 ID 基因） | 用订单号也能定位到用户所在分片，免全路由 |
| **查找表** | 单独维护 key→分片映射 | 灵活；多一跳查询 |

```java
// 典型：订单按 user_id 分 8 库 8 表
int dbIndex  = userId.hashCode() % 8;
int tblIndex = (userId / 8) % 8;   // 避免库表同余导致数据倾斜
```

### 分布式 ID

自增 ID 跨库会重复，替换方案：

| 方案 | 特点 |
|------|------|
| **雪花算法** | 时间戳+机器+序列，趋势递增（注意时钟回拨） |
| 号段模式（Leaf） | DB 批量发号段，内存分配 |
| UUID | 无序（B+ 树页分裂）、太长，索引不友好 |

### 扩容（hash 分片痛点）

```
双写迁移：
1. 上线双写（新旧分片都写），读旧
2. 存量数据迁移 + 校验对账
3. 灰度切读新分片
4. 观察稳定后停写旧
（或一开始就 32/64 库一拆到位，按 2^n 扩容只迁移一半）
```

### 拆分后的问题

| 问题 | 方案 |
|------|------|
| 跨库 join | 冗余字段 / 应用层组装 / 数据异构 ES |
| 跨库事务 | 最终一致（本地消息表 + MQ）、Seata、TCC |
| 跨库分页 | 各分片取 top N 归并；或用 ES/宽表承接复杂查询 |
| 非分片键查询 | 基因法 / 全路由广播（慢）/ 异构索引表 |

中间件：ShardingSphere-JDBC（客户端层）、MyCat（代理层）。

### 总结

```
垂直：按业务/列拆；水平：按行拆
分片键选查询最频繁的（user_id），hash 均匀 / range 易扩容
配套：雪花 ID、双写扩容、跨库事务走最终一致
复杂查询异构到 ES，别为难 MySQL
```

面试要点：拆分时机与四种方式、hash 与 range 的取舍、基因法解决非分片键查询、双写迁移扩容流程、跨库事务的最终一致方案。



## 主从延迟如何解决？

### 先量化延迟

| 手段 | 说明 |
|------|------|
| `show slave status\G` | 看 **Seconds_Behind_Master**（从库落后主库的秒数） |
| pt-heartbeat | 主库定时写心跳，从库算差值，比 Seconds_Behind_Master 更准 |

### 延迟的根本原因

```
主库：多线程并发写
从库：SQL 线程（单线程/低并行度）重放 relay log
     追不上 → 延迟越来越大
```

| 原因 | 说明 |
|------|------|
| **重放并行度不足**（根因） | 早期 SQL 线程单线程，主库 32 并发写根本追不上 |
| **大事务** | 一条 binlog 是完整事务，从库要整体重放，期间后续消息排队 |
| **从库压力大** | 从库还承担大量读查询、备份任务，CPU/IO 被抢 |
| **机器/配置差** | 从库硬件低于主库、参数未调（如 sync_master_info 落盘策略） |
| **网络传输慢** | binlog 传输带宽不足（跨机房尤其明显） |
| **表无主键** | ROW 格式按整行匹配更新，重放极慢 |
| **从库上的 DDL/锁等待** | 重建表、锁表操作阻塞 SQL 线程 |

### 解决：从库端（提升追平能力）

| 手段 | 说明 |
|------|------|
| **并行复制 MTS** | 5.7 按事务组（LOGICAL_CLOCK）并行；**8.0 WRITESET** 基于行冲突检测并行，效果最好 |
| `replica_parallel_workers` | 调大并行 worker 数（8.0 默认 4） |
| 从库专用/对等配置 | 从库硬件不低于主库；备份任务换到延迟从库 |
| **表必须有主键** | ROW 复用主键定位行，避免全行扫描 |
| 关闭从库 binlog | 无级联需求的从库 `log_slave_updates=off`，省一次落盘 |

### 解决：主库与应用端（让业务感知不到延迟）

| 手段 | 说明 |
|------|------|
| **强制读主** | 写后 N 秒内该用户的读走主库：session 标记（如 Redis 记 uid 时间戳）或中间件路由（ShardingSphere/ProxySQL） |
| **半同步复制** | 至少一个从库收到 binlog 才返回提交，延迟窗口变小（性能有损，可超时降级异步） |
| **大事务拆小** | 批量更新改成小批次，缩短单条 binlog 重放时间 |
| 业务兜底 | 写后读不到 → 前端重试/等待提示 |
| 缓存承接 | 写库同时写 Redis，读先走缓存，天然绕开延迟窗口 |

### 延迟下的读写分离取舍

```
方案A：强制读主        → 简单可靠，主库压力大（高频写后读全压主库）
方案B：写后 N 秒读主    → 平衡（session 粘性），主流做法
方案C：读从库 + 失败重试 → 性能最好，体验依赖重试逻辑
方案D：读写全走缓存     → 一致性由缓存过期策略保证
```
